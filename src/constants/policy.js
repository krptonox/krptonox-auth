export const DEFAULT_PASSWORD_MIN_LENGTH = 8;
export const DEFAULT_PASSWORD_MAX_LENGTH = 128;

export const DEFAULT_REQUIRE_UPPERCASE = false;
export const DEFAULT_REQUIRE_LOWERCASE = false;
export const DEFAULT_REQUIRE_NUMBER = false;
export const DEFAULT_REQUIRE_SPECIAL = false;